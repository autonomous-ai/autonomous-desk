# STL Files

Ready to 3D print.

## Print Settings (default)

- Material: PLA or PETG
- Layer height: 0.2 mm
- Infill: 30%
- Walls: 3
- Supports: only where noted in the file name (e.g. `bracket_v1_supports.stl`)

Override per part if needed — add a `print-notes.md` next to the STL.
